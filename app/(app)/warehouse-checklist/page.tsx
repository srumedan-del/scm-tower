import { supabase } from '@/lib/supabase'
export default async function Checklist(){const {data}=await supabase.from('warehouse_checklist').select('*').order('checklist_date',{ascending:false}).limit(30);return <div><h1 className="text-3xl font-bold mb-2">Warehouse Checklist</h1><p className="text-muted mb-6">Checklist kesiapan gudang harian.</p><div className="bg-white border border-border rounded-xl overflow-auto"><table className="w-full text-sm"><thead className="bg-surface"><tr><th className="p-3 text-left">Tanggal</th><th className="p-3 text-left">Warehouse</th><th className="p-3 text-left">Shift</th><th className="p-3 text-right">Completion</th><th className="p-3 text-left">Issue</th></tr></thead><tbody>{data?.map((r:any)=><tr key={r.id} className="border-t border-border"><td className="p-3">{r.checklist_date}</td><td className="p-3">{r.warehouse_code}</td><td className="p-3">{r.shift}</td><td className="p-3 text-right">{Number(r.completion_rate ?? 0).toFixed(0)}%</td><td className="p-3">{r.issue_notes}</td></tr>)}</tbody></table></div></div>}
